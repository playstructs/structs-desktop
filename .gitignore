# Build artifacts (rebuilt by `make sync`)
frontend/css/
frontend/fonts/
frontend/img/
frontend/lottie/
frontend/structicons/
frontend/js/
frontend/js/workers/
# SUI behaviour modules, copied from the webapp submodule by sync.sh.
frontend/sui/
.build-tmp/

# Static board harness (scripts/make_harness.sh) — must never ship in frontendDist
frontend/_harness.html
frontend/_fixtures.js

# Rust build
src-tauri/target/

# Node
node_modules/

# Reference repos (cloned for local study only)
reference/

# Claude Code session data
.claude/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Tauri generated schemas (rebuilt on build)
src-tauri/gen/

# MCP config contains bearer token — don't commit
# Users generate their own .mcp.json from the Debug tab
.mcp.json

# Icon source (keep generated sizes, not the 1024px source)
src-tauri/icons/Structs Icon.png
frontend/_harness_raid.html
frontend/_fixtures_raid.js
